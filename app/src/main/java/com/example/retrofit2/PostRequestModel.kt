package com.example.retrofit2

class PostRequestModel(private var name: String) {

    override fun toString(): String {
        return "PostRequestModel{" +
                "name='" + name + '\'' +
                '}'
    }
}